name: Update VPN Keys

on:
  schedule:
    - cron: '0 6 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install aiohttp

      - name: Run collector
        run: python vless_collector.py

      - name: Save keys
        run: |
          git config user.name "github-actions"
          git config user.email "actions@github.com"
          git add vless_keys.txt
          git commit -m "Update keys" || echo "No changes"
          git push
